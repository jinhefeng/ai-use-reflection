interface:
  display_name: "AI 使用复盘"
  short_description: "评估 AI 干预效能并提升协作水平"
  default_prompt: "Use $ai-use-reflection to detect whether this session needs a review, assess my intervention efficacy and task contribution, and rewrite weak prompts into next-session experiments."

policy:
  allow_implicit_invocation: true
