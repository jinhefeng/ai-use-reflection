interface:
  display_name: "AI 使用复盘"
  short_description: "跨会话复盘与长期 AI 协作成长档案"
  default_prompt: "Use $ai-use-reflection to review this session, save it to the resolved user archive, and refresh the HTML dashboard."

policy:
  allow_implicit_invocation: true
